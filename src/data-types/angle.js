import {Unit} from "consants";
import {unit} from "utils";

/* Units */

export const deg = (value) => unit(value, Unit.DEG);
export const grad = (value) => unit(value, Unit.GRAD);
export const rad = (value) => unit(value, Unit.RAD);
export const turn = (value) => unit(value, Unit.TURN);
